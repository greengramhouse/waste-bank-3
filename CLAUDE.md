# CLAUDE.md — EcoPink ธนาคารขยะรักษ์โลก

คู่มือสำหรับ Claude Code ในโปรเจกต์นี้ อ่านก่อนแก้โค้ดทุกครั้ง
แผนการย้ายฐานข้อมูลฉบับเต็มอยู่ที่ [`docs/plan.md`](./docs/plan.md)

---

## 1. โปรเจกต์นี้คืออะไร

ระบบธนาคารขยะของโรงเรียนชุมชนวัดไทยงาม ครูรับฝากขยะจากนักเรียน คิดเป็นเงินและแต้ม
นักเรียนถอนเงินได้ และดูสรุป/รายงานรายคน รายห้อง รายครู

ผู้ใช้จริง: นักเรียน 284 คน, บุคลากร 19 คน, 11 ห้อง (อ.2, อ.3, ป.1–ป.6, ม.1–ม.3)

---

## 2. Stack

| ชั้น | เทคโนโลยี |
|---|---|
| หน้าเว็บ | HTML ไฟล์เดียวจบ ไม่มี build step ไม่มี bundler ไม่มี framework |
| CSS | Tailwind CSS ผ่าน CDN (`cdn.tailwindcss.com`) + ธีมสี `vintage-*` ที่กำหนดใน `tailwind.config` inline |
| ไอคอน / ฟอนต์ | Font Awesome 6.4, Google Fonts (Charm, Sarabun) |
| Dialog | SweetAlert2 v11 |
| กราฟ | Chart.js |
| PDF | jsPDF + jspdf-autotable (ฝังฟอนต์ไทย Sarabun เป็น base64 ใน `initPdfFonts()`) |
| ฐานข้อมูล | **Cloud Firestore** (กำลังย้ายมาจาก Google Sheets) |
| Auth | **Firebase Authentication** — Email/Password ด้วยอีเมลสังเคราะห์ `{username}@ecopink.local` |
| สิทธิ์ | Firestore Security Rules + **custom claims** (`role`, `rooms`) ตั้งจากสคริปต์ admin ในเครื่อง |
| โฮสต์ | GitHub Pages (static ล้วน) |
| แผนค่าใช้จ่าย | Firebase **Spark (ฟรี)** — ไม่มี Cloud Functions ไม่มี backend server |

**สำคัญ: ต้องใช้ Firebase SDK แบบ `compat` เท่านั้น**
```html
<script src="https://www.gstatic.com/firebasejs/10.14.1/firebase-app-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.14.1/firebase-auth-compat.js"></script>
<script src="https://www.gstatic.com/firebasejs/10.14.1/firebase-firestore-compat.js"></script>
```
เพราะโค้ดเดิมเรียกฟังก์ชันผ่าน `onclick="switchTab('deposit')"` ซึ่งต้องการฟังก์ชันใน global scope
ถ้าใช้ modular SDK (`type="module"`) ปุ่มทุกปุ่มในหน้าจะพังทันที

---

## 3. ไฟล์และขอบเขต

| ไฟล์ | สถานะ | หมายเหตุ |
|---|---|---|
| **`app.html`** | **ไฟล์เดียวที่แก้ได้** | ระบบใหม่บน Firestore — หน้าครู/แอดมิน HTML+CSS+JS รวมในไฟล์เดียว |
| `index.html` | **อย่าแตะ** | ระบบเดิมบน Google Sheets ยังใช้งานจริงอยู่ เก็บไว้เทียบและย้อนกลับได้ |
| `student.html` | **อย่าแตะ** | หน้านักเรียน ยังยิงไป Apps Script/ชีตเดิม จะย้ายในเฟสหลัง |
| `legacy/appscript.js` | **อย่าแตะ** | backend เดิม เก็บไว้เป็น fallback ระหว่างช่วงเปลี่ยนผ่าน |
| `firebase/firestore.rules` | แก้ได้ | กฎความปลอดภัย — แก้แล้วต้อง `npm run deploy:rules` |
| `firebase/firestore.indexes.json` | แก้ได้ | ดัชนีผสม เพิ่มเมื่อมี query รูปแบบใหม่ |
| `tools/*.mjs`, `tools/accounts.json` | แก้ได้ | สคริปต์ย้ายข้อมูล/สร้างบัญชี รันในเครื่อง ไม่ได้ deploy |
| `docs/plan.md`, `README.md` | เอกสาร | แผนงานเต็ม และวิธีเริ่มใช้งาน |

**`index.html` ต้องอยู่ที่ root** เพราะ GitHub Pages เสิร์ฟเป็นหน้าแรก ย้ายเข้าโฟลเดอร์แล้วเว็บที่ไลฟ์อยู่พังทันที
ระหว่างพัฒนาเข้าระบบใหม่ที่ `/app.html` ควบคู่กับของเดิมได้ ตอนตัดระบบค่อยสลับชื่อไฟล์

**ห้ามแตกไฟล์ `app.html` ออกเป็นหลายไฟล์** — ไม่มี build step และ GitHub Pages เสิร์ฟ static ตรง ๆ
การแยกไฟล์จะสร้างภาระ deploy โดยไม่ได้อะไรกลับมา

---

## 4. โครงสร้างข้อมูล Firestore (ย่อ)

รายละเอียดเต็มอยู่ใน `docs/plan.md` ส่วนที่ต้องจำ:

```
members/{memberId}          นักเรียน + บุคลากรรวมกัน แยกด้วย type: 'student' | 'staff'
rooms/{roomName}            11 ห้อง มี order สำหรับเรียง + teacherUids/teacherNames/teacherMemberIds
users/{uid}                 บัญชีผู้ใช้ role: 'teacher'|'admin', rooms: [], memberId
wasteTypes/{typeId}         ประเภทขยะ ราคา/กก. แต้ม/กก.
transactions/{autoId}       ธุรกรรม (ดูฟิลด์วันที่ในหัวข้อ 6)
deletedTransactions/{id}    รายการที่ถูกลบ เก็บไว้ตรวจย้อนหลัง
balances/{memberId}         ยอดสรุปรายคน อัปเดตด้วย FieldValue.increment()
```

หลักที่ยึด:
- **`memberId` = รหัสนักเรียน/รหัสบุคลากร ใช้เป็น document ID ตรง ๆ** ไม่ใช้ auto-id
- **`roomName` = ชื่อห้องภาษาไทย** (`"ป.5"`) ใช้เป็น document ID ตรง ๆ
- **ทุกธุรกรรมต้องมี `room`** เพราะ Security Rules กรองด้วยฟิลด์นี้
- **บุคลากรสังกัดห้องที่ตัวเองเป็นครูประจำชั้น** ไม่ใช่ห้องลอย ๆ — ถ้าครู ป.5 มี `room: "ส่วนกลาง"` กฎจะมองว่าเขียนข้ามห้อง แล้วครูจะรับฝากขยะในชื่อตัวเองไม่ได้ (ผอ./ธุรการ ที่ไม่ได้คุมห้อง อยู่ `"ส่วนกลาง"` ให้แอดมินดูแล)
- **`balances` ต้องมีฟิลด์ `room`** เพราะกฎใช้ตรวจสิทธิ์โดยไม่ต้อง `get()` ข้ามคอลเลกชัน — `get()` ในกฎเสียโควต้าอ่านทุกครั้ง
- **ห้าม `update` ธุรกรรม** — แก้ไม่ได้ ลบแล้วสร้างใหม่ (audit trail)
- เขียนหลายเอกสารพร้อมกันใช้ `writeBatch` + `FieldValue.increment()` ไม่ใช่ read-modify-write

---

## 5. ผู้ใช้และสิทธิ์

มีผู้ใช้ 2 บทบาทเท่านั้น **ไม่มีบัญชีนักเรียนในหน้านี้**

### ครู (`role: 'teacher'`)
- 1 บัญชีต่อครู 1 คน (ห้องที่มีครู 2 คน แยกเป็น 2 บัญชี)
- `rooms: ["ป.5"]` — array แม้ตอนนี้จะมีห้องเดียว เผื่ออนาคตครูสอนควบ
- **ป้อนข้อมูลได้เฉพาะห้องที่รับผิดชอบ** — รับฝาก / รับฝากด่วน / ลบรายการ ของห้องตัวเองเท่านั้น
- อ่านได้เฉพาะ `members` และ `transactions` ที่ `room` ตรงกับห้องตัวเอง
- ครูเองก็เป็น `members` ที่อยู่ห้องเดียวกับนักเรียน (`type: 'staff'`) จึงรับฝากขยะในชื่อตัวเองและครูคู่ห้องได้
- **มองไม่เห็นข้อมูลห้องอื่นเลย** ทั้งรายชื่อและธุรกรรม
- ซ่อนแท็บ `ตั้งค่าระบบ` และ `ถอนเงิน` (ค่าคงที่ `ADMIN_ONLY_TABS`)

### แอดมิน (`role: 'admin'`)
- เห็นและทำได้ทุกอย่างทุกห้อง
- ผู้เดียวที่แก้ `members`, `wasteTypes`, `rooms` และถอนเงินได้

### กติกาการเขียนโค้ดที่ห้ามลืม
```js
// ครูต้องใส่ where('room','==',myRoom) เสมอ
// ไม่งั้น Firestore ปฏิเสธทั้ง query (list rules ตรวจจาก query constraint ไม่ใช่ทีละเอกสาร)
db.collection('transactions')
  .where('schoolYear','==', sy)
  .where('room','==', myRoom)      // ← ห้ามลืมสำหรับ role teacher
```

**สิทธิ์ต้องบังคับที่ Security Rules เสมอ** การซ่อนปุ่ม/กรองใน JavaScript เป็นแค่ UX
`app.html` อยู่บน GitHub แบบ public ใครก็เปิด DevTools แก้ตัวแปรได้

### ฟีเจอร์ในอนาคต — "ภาพรวมทั้งโรงเรียน" สำหรับครู
มีแผนจะเปิดให้ครูดูภาพรวมทั้งโรงเรียนได้ (อ่านอย่างเดียว) เวลาออกแบบให้เผื่อไว้:
- **แยก "สิทธิ์เขียน" ออกจาก "สิทธิ์อ่าน" ในโค้ดตั้งแต่ตอนนี้** อย่าใช้เงื่อนไข `currentRole === 'admin'` ปนกันสองความหมาย
  ใช้ helper แยก เช่น `canWriteRoom(room)` กับ `canViewRoom(room)`
- แท็บกลุ่มสรุป/รายงาน ให้ผ่าน `canViewRoom()` ส่วนแท็บรับฝาก/ถอน/ลบ ให้ผ่าน `canWriteRoom()`
- เปิดฟีเจอร์นี้ทีหลังจะเป็นการเพิ่ม claim `canViewAll: true` + ผ่อน Rules ฝั่ง read อย่างเดียว ไม่ต้องรื้อ UI
- **อย่าให้ครูอ่านทั้งโรงเรียนโดยดีฟอลต์** เพราะจะกินโควต้าอ่านของ Spark และเป็นข้อมูลเด็กคนอื่น

---

## 6. การจัดการวันที่ (สำคัญที่สุด — อ่านให้ครบ)

ระบบนี้ค้นหาและออกรายงานตามช่วงวันที่ตลอด และผู้ใช้อยู่ในไทย (UTC+7) แต่แสดงผลเป็น **พ.ศ.**
เรื่องวันที่จึงเป็นจุดที่พังง่ายที่สุด กติกาด้านล่างนี้บังคับใช้ทั้งไฟล์

### 6.1 เก็บอะไรใน Firestore

ทุกเอกสาร `transactions` เก็บวันที่ **สองฟิลด์ที่ทำหน้าที่ต่างกัน ห้ามใช้สลับกัน**

```js
{
  date:      "2026-09-03",              // string 'YYYY-MM-DD' = "วันทำการ" ตามปฏิทินไทย
  createdAt: FieldValue.serverTimestamp(),  // Timestamp = "เวลาที่กดบันทึกจริง"
  schoolYear: "2569"                    // string ปีการศึกษา
}
```

| ฟิลด์ | ชนิด | ใช้ทำอะไร | ห้ามใช้ทำอะไร |
|---|---|---|---|
| `date` | `string` | **กรองช่วงวันที่ทุกกรณี**, จัดกลุ่มรายวันในรายงาน, แสดงวันที่ | ไม่ใช้จับเวลา |
| `createdAt` | `Timestamp` | เรียงลำดับล่าสุด→เก่าสุด, แสดง**เวลา** (ชม.:นาที) ในตารางประวัติ | **ห้ามใช้กรองช่วงวันที่** |
| `schoolYear` | `string` | จำกัดขอบเขต query ไม่ให้ดึงข้อมูลข้ามปี | — |

**ทำไม `date` ต้องเป็น string ไม่ใช่ Timestamp**
ครูเลือกวันที่จาก `<input type="date">` ซึ่งให้ค่า `"2026-09-03"` — เป็น **วันตามปฏิทิน** ไม่ใช่จุดเวลา
ถ้าแปลงเป็น `Timestamp` จะต้องเลือกว่าเที่ยงคืนของโซนไหน แล้วเวลาอ่านกลับมาก็ต้องแปลงกลับ
ทุกจุดที่แปลงคือโอกาสที่วันจะเลื่อนไป 1 วัน ส่วน string ISO เทียบกันได้ตรง ๆ และเรียงตามตัวอักษรได้ถูกต้องอยู่แล้ว

### 6.2 Query ช่วงวันที่

```js
// ISO string เรียงตามตัวอักษร = เรียงตามเวลา จึงใช้ >= <= ได้เลย
db.collection('transactions')
  .where('schoolYear','==', schoolYear)
  .where('room','==', room)              // teacher เท่านั้น; admin ตัดบรรทัดนี้
  .where('date','>=', startStr)          // "2026-09-01"
  .where('date','<=', endStr)            // "2026-09-30"
  .orderBy('date','desc')
  .orderBy('createdAt','desc')
```
Composite index ที่ต้องมี: `schoolYear ASC, room ASC, date DESC, createdAt DESC`

ข้อจำกัด Firestore ที่ต้องรู้: **field ที่ใช้ range filter (`date`) ต้องเป็น `orderBy` ตัวแรก**
จะ `orderBy('createdAt')` ก่อน `date` ไม่ได้

### 6.3 ห้ามทำ / ให้ทำ

| ❌ ห้าม | เพราะ | ✅ ให้ใช้แทน |
|---|---|---|
| `new Date("2026-09-03")` | พาร์สเป็น **UTC เที่ยงคืน** ไม่ใช่เวลาไทย | เทียบ string ตรง ๆ `a >= b` |
| `new Date(t.date) >= startDate` | โค้ดเดิมใน `generateReport()` ทำแบบนี้ รอดเพราะไทยเป็น UTC+7 พอดี **แต่พังทันทีถ้าเครื่องตั้งโซนเวลาอื่น** | `t.date >= startStr && t.date <= endStr` |
| `new Date().toISOString().slice(0,10)` | ให้วันตาม **UTC** — ก่อน 07:00 น. ไทย จะได้วันของเมื่อวาน | `todayBangkok()` (ข้อ 6.4) |
| `dates.sort((a,b) => new Date(b) - new Date(a))` | สร้าง Date object โดยไม่จำเป็น | `dates.sort().reverse()` |
| เก็บ `date` เป็น `Timestamp` | ต้องแปลงโซนเวลาไปกลับ วันเลื่อนได้ | เก็บเป็น string `'YYYY-MM-DD'` |

### 6.4 Helper กลางที่ต้องมี (เขียนครั้งเดียว ใช้ทั้งไฟล์)

```js
const TZ = 'Asia/Bangkok';

/** วันนี้ตามเวลาไทย เป็น 'YYYY-MM-DD' — ถูกต้องแม้เครื่องผู้ใช้ตั้งโซนเวลาผิด */
function todayBangkok() {
  return new Intl.DateTimeFormat('en-CA', { timeZone: TZ }).format(new Date());
}

/** Firestore Timestamp | Date | string → 'YYYY-MM-DD' ตามเวลาไทย */
function toDateStr(v) {
  if (!v) return '';
  if (typeof v === 'string' && /^\d{4}-\d{2}-\d{2}$/.test(v)) return v;   // ผ่านมาถูกอยู่แล้ว
  const d = v.toDate ? v.toDate() : new Date(v);                          // รองรับ Timestamp
  return isNaN(d) ? '' : new Intl.DateTimeFormat('en-CA', { timeZone: TZ }).format(d);
}

/** 'YYYY-MM-DD' → 'DD/MM/YY' พ.ศ. สำหรับหัวตารางรายงานที่พื้นที่จำกัด */
function toThaiShort(s) {
  const [y, m, d] = s.split('-');
  return `${d}/${m}/${String(+y + 543).slice(2)}`;
}

/** 'YYYY-MM-DD' → '3 กันยายน 2569' สำหรับหัวรายงาน/PDF */
function toThaiLong(s) {
  const TH = ['มกราคม','กุมภาพันธ์','มีนาคม','เมษายน','พฤษภาคม','มิถุนายน',
              'กรกฎาคม','สิงหาคม','กันยายน','ตุลาคม','พฤศจิกายน','ธันวาคม'];
  const [y, m, d] = s.split('-');
  return `${+d} ${TH[+m - 1]} ${+y + 543}`;
}

/** เวลา 'HH:mm' จาก createdAt (Timestamp) — ใช้ในคอลัมน์เวลาของตารางประวัติ */
function toTimeStr(ts) {
  if (!ts) return '';
  const d = ts.toDate ? ts.toDate() : new Date(ts);
  return isNaN(d) ? '' : d.toLocaleTimeString('th-TH',
    { timeZone: TZ, hour: '2-digit', minute: '2-digit' });
}

/**
 * ปีการศึกษาไทยของวันที่หนึ่ง ๆ — เปิดเทอมกลางพฤษภาคม
 * ม.ค.–เม.ย. ยังนับเป็นปีการศึกษาก่อนหน้า
 * เช่น 2026-09-03 → "2569", 2027-02-10 → "2569"
 */
function schoolYearOf(s) {
  const [y, m] = s.split('-').map(Number);
  return String(m >= 5 ? y + 543 : y + 542);
}
```

**ปีการศึกษา (พ.ศ.) ≠ ปีปฏิทิน** — ปีการศึกษา 2569 กินตั้งแต่ พ.ค. 2026 ถึง มี.ค. 2027
ต้องคำนวณจาก `schoolYearOf()` เสมอ ห้ามใช้ `year + 543` ตรง ๆ

### 6.5 การแสดงผล

- **แสดงต่อผู้ใช้เป็น พ.ศ. เสมอ** (ค.ศ. + 543) — ห้ามโชว์ ค.ศ. ในหน้าจอหรือ PDF
- `<input type="date">` เป็น native control **แสดง ค.ศ. เสมอ แก้ไม่ได้** →
  ให้แสดงข้อความ พ.ศ. กำกับใต้/ข้างช่องนั้น เช่น `3 ก.ย. 2569` เพื่อกันครูสับสน
- ตารางประวัติแสดง **เวลา** จาก `createdAt` แต่ **กรองด้วย `date`** — สองอย่างนี้ต่างกัน
  รายการที่ครูย้อนบันทึกของเมื่อวาน จะมี `date` เป็นเมื่อวาน แต่ `createdAt` เป็นวันนี้ ถูกต้องแล้ว
- ค่าเริ่มต้นของทุกช่องวันที่ = `todayBangkok()` (โค้ดเดิมทำถูกอยู่แล้วที่ `app.html` todayBangkok() — สร้างจาก component ท้องถิ่น ไม่ใช่ `toISOString()`)

---

## 7. ตรรกะธุรกิจที่ต้องรักษาไว้

| กติกา | ที่มา |
|---|---|
| ยอดเงิน = ผลรวม `amount` ทุกธุรกรรมของคนนั้น (ฝากเป็นบวก ถอนเป็นลบ) | `getBalanceRows()` |
| **ถอนเงินได้เมื่อยอดคงเหลือ ≥ 50 บาท** และถอนเกินยอดไม่ได้ | `MIN_WITHDRAW_BALANCE` |
| รายการถอนเก็บ `type: 'withdraw'`, `wasteTypeName: "ถอนเงินออก"`, `amount` ติดลบ, `weight`/`points` ว่าง | `submitWithdraw()` |
| รายงานต้อง **ไม่นับรายการถอน** เป็นน้ำหนัก/รายได้ | `generateReport()` กรอง `wasteType === "ถอนเงินออก"` — ของใหม่ให้กรองด้วย `type === 'deposit'` แทน (ชัดกว่า) |
| รับฝากด่วนบันทึกได้สูงสุด 30 คนต่อครั้ง | `submitAllFastDeposit()` |
| แต้ม ปัดลงเป็นจำนวนเต็มด้วย `Math.floor()` | `submitAllFastDeposit()` |
| รายงานสรุปนับเฉพาะนักเรียน ไม่รวมบุคลากร | `students.filter(s => !s.isTeacher)` |

---

## 8. UI / UX และ Responsive

หน้าจอปัจจุบันออกแบบมาสำหรับเดสก์ท็อป แต่ครูใช้งานจริงบนมือถือเป็นหลัก
**เมื่อแก้ส่วนไหนของ UI ให้ยกระดับส่วนนั้นตามเกณฑ์ด้านล่างไปด้วย** ไม่ต้องรื้อทั้งหน้าในครั้งเดียว

### 8.1 หลักการ
- **Mobile first** ทดสอบที่ความกว้าง **360px** เป็นเกณฑ์ต่ำสุด แล้วค่อยขยายด้วย `sm:` `md:` `lg:`
- **หน้าเว็บต้องไม่เลื่อนแนวนอน** ตารางกว้าง ๆ ต้องอยู่ใน `overflow-x-auto` ของตัวเอง
- **เป้ากดต้องไม่ต่ำกว่า 44×44px** ปุ่มลบไอคอนเล็ก ๆ ในตารางปัจจุบันเล็กเกินไปสำหรับนิ้ว
- ใช้คลาส Tailwind ที่มีอยู่ (ธีม `vintage-*`) อย่าเพิ่มไลบรารี UI ใหม่
- ข้อความทุกอย่างเป็นภาษาไทย

### 8.2 สิ่งที่ทำไปแล้วใน `app.html` — อย่ารื้อ

| เดิม | ตอนนี้ |
|---|---|
| แท็บ 10 อันเรียงแนวนอน ต้องเลื่อนหา แท็บท้าย ๆ มองไม่เห็น | ยุบเหลือ **5 หัวข้อ**: ฝาก/ถอน ▾ · ค้นหา ▾ · สรุป ▾ · รายงาน · ตั้งค่า |
| ตารางกว้างเกินจอมือถือ | คลาส `stack-mobile` + `data-label` ที่ทุก `<td>` → ต่ำกว่า 640px พับเป็นการ์ด (ใช้กับตารางประวัติ, ค้นหายอด, สรุปรายวันห้อง/ครู) |
| `itemsPerPage = 5` คงที่ | `pageSize()` คืน 8 / 10 / 12 ตามความกว้างจอ และคำนวณใหม่ตอนหมุนจอ |
| ช่องวันที่แสดง ค.ศ. | `attachThaiDateHints()` เติมบรรทัด พ.ศ. ใต้ทุก `<input type="date">` อัตโนมัติ |
| หน้ารายงานต้องกดปฏิทินสองครั้งทุกที | ปุ่มลัด `setReportRange()` — วันนี้ / 7 วันล่าสุด / เดือนนี้ / ปีการศึกษานี้ |
| ตารางสรุปรายวันและแกน X ของกราฟเป็น ค.ศ. | ผ่าน `toThaiLong()` / `toThaiShort()` เป็น พ.ศ. ทั้งหมด |
| บันทึกรับฝากสำเร็จขึ้น modal ต้องกด OK ทุกครั้ง | `toastOk()` มุมจอ หายเอง — **modal ยังเก็บไว้ให้ถอนเงินและลบ** ซึ่งเป็นเรื่องสำคัญ |
| โหลดข้อมูลพลาดแล้วเงียบ เห็นตารางว่างเปล่า | `loadTransactions()` ขึ้น toast แดงบอกสาเหตุ แล้ววาดส่วนที่โหลดสำเร็จต่อ |
| หัวเว็บล้นจอที่ 375px ชื่อแอปทับชื่อผู้ใช้ | ย่อโลโก้ ซ่อนชื่อแอปต่ำกว่า `xs:` (420px — breakpoint ที่เพิ่มเองใน `tailwind.config`) และใส่ `min-w-0` / `shrink-0` ให้ถูกฝั่ง |
| ชื่อผู้ใช้ถูกซ่อนทั้งก้อนบนมือถือ | แสดงเสมอแต่ย่อขนาด — สำคัญเพราะห้องหนึ่งมีครูสองคนใช้เครื่องเดียวกัน |
| ช่องตัวเลขเรียกแป้นตัวอักษรเต็มบนมือถือ | `inputmode="decimal"` ทุกช่องน้ำหนัก/จำนวนเงิน |

### 8.2.1 โครงเมนู — แก้ที่เดียวเสมอ

รายการเมนูอยู่ที่อาร์เรย์ `TABS` (id, label, short, icon, adminOnly)
การจัดกลุ่มอยู่ที่ `NAV_GROUPS` แยกออกมาต่างหาก แล้ว `renderNav()` วาดออกเป็น 3 ที่:

| ที่ | เมื่อไหร่ | หน้าตา |
|---|---|---|
| `#tabBar` | `md:` ขึ้นไป | 5 หัวข้อ กลุ่มที่มีสมาชิกหลายคนเปิดเป็น dropdown (คลิก ไม่ใช่ hover), กลุ่มสมาชิกเดียวเป็นปุ่มตรง ๆ |
| `#bottomNav` | ต่ำกว่า `md:` | แถบล่างติดขอบจอ — 4 เมนูแรกที่ผู้ใช้คนนั้นเห็น + ปุ่ม "เมนู" |
| `#menuSheet` | ต่ำกว่า `md:` | แผงเลื่อนขึ้นจากล่าง จัดกลุ่มเหมือนแถบบน (กลุ่มสมาชิกเดียวรวมไว้ใต้ "อื่น ๆ") |

**เพิ่มเมนูใหม่:** ใส่ใน `TABS` แล้วเติม id ลงในกลุ่มที่ต้องการใน `NAV_GROUPS` — เท่านี้ทั้งสามที่อัปเดตเอง
ห้ามเขียนปุ่มเมนูลงใน HTML ตรง ๆ อีก เพราะจะหลุดจากอีกสองที่ทันที

คลาสที่ `switchTab()` ใช้ไฮไลต์ (อย่าสลับกัน):
- `.navgrp` = หัวข้อกลุ่มบนแถบบน + ปุ่มกลุ่มสมาชิกเดียว → ไฮไลต์ด้วยเส้นใต้
- `.navitem` = รายการใน dropdown → ไฮไลต์ด้วยพื้นหลัง (รีเซ็ตเป็นสีเทาไม่ได้ ข้อความจะจางจนอ่านไม่ออก)
- `.btab` = ปุ่มแถบล่าง · `.mtab` = ปุ่มในแผงเมนูรวม

### 8.2.2 หน้ารับฝากด่วน — จุดที่ปรับเป็นพิเศษ

| เดิม | ตอนนี้ |
|---|---|
| ตาราง `min-w-[800px]` บังคับเลื่อนแนวนอนบนมือถือ | `sm:min-w-[800px]` + `stack-mobile` → มือถือเป็นการ์ดรายคน |
| หัวตารางเลื่อนหายเมื่อไล่กรอก 30 แถว | กรอบตารางสูงสุด `sm:max-h-[60vh]` เลื่อนในตัวเอง + `<th>` เป็น `sticky top-0` |
| ช่องกรอก `py-1.5` เล็กเกินกดพลาด | `min-h-[44px]` ตามเกณฑ์เป้ากดขั้นต่ำ |
| ปุ่มบันทึกอยู่ใต้แถวที่ 30 ต้องเลื่อนลงสุด และไม่รู้ว่ากรอกไปกี่คน | แถบสรุป+ปุ่มบันทึกเป็น `sticky bottom-16 md:bottom-0` แสดง "กรอกแล้ว N คน · รวม ฿X" สด ๆ และ **ปิดปุ่มเมื่อยังไม่กรอกอะไรเลย** |

`updateFastDepositSummary()` ต้องถูกเรียกทุกครั้งที่ตารางเปลี่ยน — ตอน render (รวมกรณีค้นหาไม่เจอ), ตอนคำนวณราคา, และตอน `finally` ของการบันทึก
**อย่าปลดล็อกปุ่มด้วย `btn.disabled = false` ตรง ๆ** ให้เรียกฟังก์ชันนี้แทน สถานะปุ่มจะได้ตรงกับข้อมูลที่กรอกไว้จริง

### 8.2.3 หน้ารายงานประจำเดือน — สองมุมมองในฟังก์ชันเดียว

ตารางรายงานเป็นตารางไขว้ (นักเรียน × วันที่) จำนวนคอลัมน์ไม่คงที่ ขึ้นกับว่าช่วงที่เลือก
มีการรับซื้อกี่ครั้ง จึงพับด้วย `stack-mobile` เหมือนตารางอื่นไม่ได้
`generateReport()` เลยวาดสองมุมมองจากข้อมูลชุดเดียวกัน:

| มุมมอง | คลาส | หน้าตา |
|---|---|---|
| จอกว้าง | `hidden sm:block` | ตารางไขว้เต็ม ทุกคนทุกคอลัมน์ เหมือนเดิมทุกอย่าง |
| มือถือ | `sm:hidden` | การ์ดรายคน โชว์ยอดรวม กด `<details>` กางดูรายละเอียดแต่ละครั้ง |

การ์ดมือถือ **แสดงเฉพาะคนที่มีรายการในช่วงนั้น** แล้วบอกท้ายรายการว่าเหลืออีกกี่คนที่ไม่มีรายการ
เพราะ 284 การ์ดที่ว่างเปล่าเกือบทั้งหมดไม่ช่วยอะไร ส่วนตารางจอกว้างและ PDF ยังแสดงครบทุกคนตามเดิม

ตัวแปร `reportRows` ถูกเก็บระหว่างวนสร้างตาราง — **แก้สูตรคำนวณที่เดียว ทั้งสองมุมมองตรงกันเสมอ**
อย่าคำนวณซ้ำแยกฝั่งมือถือ

### 8.2.4 คีย์บอร์ดในเมนู

| ที่ | ปุ่ม | ผล |
|---|---|---|
| หัวข้อกลุ่ม (`.navgrp`) | ↓ / Enter / Space | เปิด dropdown แล้วโฟกัสรายการแรก |
| หัวข้อกลุ่ม | ↑ | เปิด dropdown แล้วโฟกัสรายการสุดท้าย |
| รายการใน dropdown | ↑ ↓ | เลื่อนขึ้นลง วนรอบเมื่อเลยหัว/ท้าย |
| รายการใน dropdown | Home / End | ไปรายการแรก / สุดท้าย |
| รายการใน dropdown | Esc | ปิดแล้ว**คืนโฟกัสให้หัวข้อกลุ่ม** ไม่ทิ้งโฟกัสลอย |
| แผงเมนูรวม (มือถือ) | Tab / Shift+Tab | วนอยู่ในแผง ไม่หลุดไปข้างหลัง (focus trap) |
| แผงเมนูรวม | Esc | ปิดแล้วคืนโฟกัสให้ปุ่มที่เปิด (`menuSheetLastFocus`) |

`aria-expanded` ของหัวข้อกลุ่มต้องอัปเดตทั้งตอนเปิดและตอนปิด — `closeNavGroups()` ตั้งเป็น `false` ให้ทุกปุ่ม

**`openMenuSheet()` ย้ายโฟกัสทันที ไม่รอ `requestAnimationFrame`** — rAF มีไว้ทำอนิเมชันอย่างเดียว
เบราว์เซอร์หน่วง rAF ได้ ถ้าผูกโฟกัสไว้ด้วยกันโฟกัสจะค้างอยู่ข้างหลังแผงที่เปิดอยู่

### 8.3 ที่ยังไม่ได้ทำ

ยังไม่มีรายการค้าง — ถ้าเจอปัญหา responsive ใหม่ ให้บันทึกไว้ตรงนี้

### 8.4 การเข้าถึงและความชัดเจน
- อย่าสื่อสารสถานะด้วยสีอย่างเดียว — เขียว/แดงของยอดเงินให้มีเครื่องหมาย `+` / `-` กำกับด้วย (โค้ดเดิมทำถูกแล้ว)
- ทุกช่องกรอกต้องมี `<label>` หรือ `aria-label` ไม่ใช้ placeholder แทน label
- ข้อความ error ต้องบอกว่าต้องทำอะไรต่อ ไม่ใช่แค่บอกว่าผิด

---

## 9. ธรรมเนียมการเขียนโค้ด

- **คอมเมนต์เป็นภาษาไทย** ตามของเดิม อธิบาย *ทำไม* ไม่ใช่ *ทำอะไร*
- ตั้งชื่อฟังก์ชันเป็น camelCase อังกฤษ, ข้อความ UI เป็นไทย
- **ห้ามเปลี่ยนชื่อฟังก์ชันเดิมโดยไม่จำเป็น** — มี `onclick=` ใน HTML ผูกกับชื่ออยู่
- คั่นหมวดหมู่ด้วยคอมเมนต์รูปแบบเดิม `// ================= D E P O S I T =================`
- ไม่มี test runner ไม่มี linter — ตรวจด้วยการเปิดหน้าเว็บจริงและ Firestore emulator

---

## 10. คำสั่งที่ใช้บ่อย

```bash
# เปิดหน้าเว็บทดสอบในเครื่อง (ต้องเสิร์ฟผ่าน http ไม่ใช่ file:// ไม่งั้น Firebase Auth ไม่ทำงาน)
npx serve .

# Firestore + Auth emulator
firebase emulators:start --only firestore,auth

# ย้ายข้อมูลจากชีต (รันครั้งเดียว)
node tools/export-sheets.mjs
node tools/seed-firestore.mjs          # เพิ่ม --emulator เพื่อยิงเข้า emulator
node tools/verify.mjs

# deploy เฉพาะ rules/indexes (หน้าเว็บ deploy ผ่าน git push ไป GitHub Pages)
firebase deploy --only firestore:rules,firestore:indexes
```

---

## 11. กับดักที่เจอมาแล้ว

1. **`localeCompare` เรียงห้องไทยผิด** — `"อ.2"` ไปอยู่ท้ายสุดแทนที่จะอยู่หน้าสุด → ใช้ฟิลด์ `order` ใน `rooms` แทนเสมอ
2. **ชื่อครูในข้อมูลเดิมสะกดไม่นิ่ง** — มีช่องว่างซ้อน (`"ครูเสฏฐวุฒิ  นิลกระ"`), มีคำนำหน้า "ครู" ติดมา, นามสกุลสะกดต่างกัน (เสาวงษ์/เสาวงศ์) → **ห้ามจับคู่ครูด้วยการเทียบชื่อ** ใช้ `uid` / `memberId` เท่านั้น
3. **Custom claims ไม่อัปเดตทันที** — เปลี่ยน role/rooms แล้ว token เดิมค้างได้ถึง 1 ชม. ต้องให้ผู้ใช้ล็อกเอาต์แล้วเข้าใหม่ หรือเรียก `getIdToken(true)`
4. **โควต้า Spark: อ่าน 50,000 / เขียน 20,000 ต่อวัน** — ทุก query ต้องมี `schoolYear` และ (สำหรับครู) `room` เสมอ อย่าดึงทั้ง collection
5. **`student.html` ยังอ่านจาก Google Sheets** — ระหว่างช่วงเปลี่ยนผ่าน แต้มและประวัติของนักเรียนจะไม่ตรงกับที่ครูบันทึก เป็นเรื่องที่รู้อยู่แล้ว ไม่ต้อง "แก้"
6. **`onclick=` ต้องการ global function** — อย่าห่อโค้ดใน IIFE, module, หรือ `DOMContentLoaded` โดยไม่ผูกกลับเข้า `window`
